global:
  scrape_interval: 5s # Scrape targets every 5 seconds for near-real-time updates

scrape_configs:
  - job_name: 'yolo_segmentation'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['api:8000']
