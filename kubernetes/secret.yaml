apiVersion: v1
kind: Secret
metadata:
  name: yolo-secrets
  namespace: yolo-segmentation
type: Opaque
stringData:
  # Placeholder for future secrets such as database credentials, API keys, etc.
  # Currently empty as the application doesn't require hardcoded secrets.
  DUMMY_SECRET: "placeholder-value"
